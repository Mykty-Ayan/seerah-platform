[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd /app/backend && ./seerah-backend"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[nixpacks]
goVersion = "1.25"

[nixpacks.build]
root = "backend"
