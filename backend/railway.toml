[build]
builder = "NIXPACKS"

[deploy]
startCommand = "./out"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[nixpacks]
goVersion = "1.25"

[nixpacks.build]
root = "backend"
